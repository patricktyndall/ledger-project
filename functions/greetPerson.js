// to test at command line: `node functions/greetPerson`

/**
 * A function that can be called like `greetPerson('hi')('Patrick')`, and
 * @returns the string 'Hi Patrick'
 */
function greetPerson(/** TODO params */) {
	// TODO
	console.log('asd');
}

function test() {
	// call greetPerson here
	const result = greetPerson('hi')('Patrick');
	console.log(result);
}

test();
